import styled from 'styled-components'

export const Container = styled.p`
  padding: 0.5em 0;
  line-height: 1.8em;
  span {
    font-weight: 700;
    text-decoration: underline;
    cursor: pointer;
  }
`
