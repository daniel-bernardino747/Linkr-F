import styled from 'styled-components'

export const Container = styled.p`
  width: 520px;
  padding: 0.3em 0;
  line-height: 1.2em;
  span {
    span {
      span {
        color: none;
        font-weight: 700;
        text-decoration: underline;
        cursor: pointer;
      }
    }
  }
`
